plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.AdrianGeovany.cookbookacevedo"
    compileSdk = 34 // Define directamente la versión del SDK

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_17.toString()
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "com.AdrianGeovany.cookbookacevedo"
        // You can update the following values to match your application needs.
        minSdk = 21 // Define directamente la versión mínima de SDK
        targetSdk = 34 // Define directamente la versión de SDK objetivo
        versionCode = 1 // Define directamente el código de versión
        versionName = "1.0" // Define directamente el nombre de la versión
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}